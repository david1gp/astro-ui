## [0.5.0] - 2026-02-12

### 🚀 Features

- *(styles)* Add dotted and slash styles
- *(styles)* Add cube, slash sparse, grid, blueprint, squre zig, wave patterns and demos
- *(meta)* Add opengraph meta generation functions to be passed into Layout component
- *(button)* Add filed blue variant
- *(icons)* Add bluesky, devTo, microsoft

### 🐛 Bug Fixes

- *(button)* W3 validator - children div not allowed in span element
- *(button)* Redo div -> span, because buttons are not allowed to have div as children
- *(bg-styles)* Make const with satisfies, remove semicolons at the end of some
- *(_headers)* Move _headers to public/ for Cloudflare Pages
- *(images)* Serve images from cf deployment instead of b2 storage, add caching for images
- *(details)* Allow overwriting classes
- *(layout)* Remove ico link from minimal layout, is most often loaded anyway

### 🚜 Refactor

- *(theme-toglle)* Remove view-transition-name, w3 validator complains about it
- *(app)* Move layout, demos, layout_parts into app subfolder
- *(layout)* Allow hiding tailwind indicator, remove apple touch icon
- *(theme-toggle)* Add prop to disable script loading, forces light mode
- *(theme-toggle)* Add prop to disable script loading, rename to includeScript
- *(card)* Use semantic article element instead of div for cards

### 🎨 Styling

- *(md)* Reduce default text size to 16 (from 18)

### ⚙️ Miscellaneous Tasks

- *(favicons)* Remove apple-touch-icon
- Chore(migrate): migrate from adaptive-shield-matrix -> david1gp, rename
adaptive-sm -> adaptive-ds
